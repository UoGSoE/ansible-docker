# Quick'n'dirty ansible to install docker

Just got bored copy'n'pasting from the docker install docs, so made a little ansible playbook container and a playbook.

See the `run.sh` for how it should be run.
